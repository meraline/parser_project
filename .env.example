# Настройки базы данных PostgreSQL
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=auto_reviews
DATABASE_USER=parser
DATABASE_PASSWORD=parser

# Тип базы данных (sqlite или postgresql)
DATABASE_TYPE=postgresql

# Путь к SQLite (если используется)
SQLITE_PATH=auto_reviews.db

# Настройки логирования
LOG_LEVEL=INFO
LOG_FILE=parser.log

# Настройки парсера
USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36
REQUEST_DELAY=1
MAX_RETRIES=3

# Настройки Chrome для Playwright
CHROME_PATH=./chrome-linux/chrome
CHROME_HEADLESS=true

# Мониторинг (опционально)
# Настройки базы данных
DATABASE_TYPE=postgresql
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=auto_reviews
DATABASE_USER=parser
DATABASE_PASSWORD=parser

# Путь к SQLite (если используется)
SQLITE_PATH=auto_reviews.db

# Настройки парсера
PROMETHEUS_PORT=8000
REDIS_URL=redis://localhost:6379/0
MAX_WORKERS=4

# Настройки логирования
LOG_LEVEL=INFO
LOG_FILE=parser.log

# Настройки Chrome для Playwright
CHROME_PATH=./chrome-linux/chrome
CHROME_HEADLESS=true

